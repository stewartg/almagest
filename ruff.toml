# See https://docs.astral.sh/ruff/rules/
exclude = ["^tests/", "__init__.py"]
line-length = 120

[lint]
select = [
    "B",  # flake8-bugbear
    "C4", # flake8-comprehensions
    "D",  # pydocstyle
    "E",  # Error
    "F",  # pyflakes
    "I",  # isort
    "N",  # pep8-naming
    "PGH",  # pygrep-hooks
    "PTH", # flake8-use-pathlib
    "Q",  # flake8-quotes
    "S",  # bandit
    "SIM",  # flake8-simplify
    "TRY",  # tryceratops
    "UP",  # pyupgrade
    "W",  # Warning
    "YTT", # flake8-2020
]
ignore = [
    "FIX001",   # line-contains-fixme
    "FIX002",   # line-contains-todo
    "G004",     # logging-f-string
    "PLR0913",  # too-many-arguments
    "PLR0914",  # too-many-locals
    "PLR0917",  # too-many-positional-arguments
    "B905",     # zip strict=True; remove once python <3.10 support is dropped.
    "UP006",    # Keep using Optional/Union instead of | (for Python <3.10 compatibility)
    "UP045",
    "D100",
    "D101",
    "D102",
    "D103",
    "D104",
    "D105",
    "D106",
    "D107",
    "D200",
    "D401",
    "E402",
    "E501",     # Line too long (often handled by formatter or legacy code)
    "F401",     # Unused imports (sometimes needed for side effects or type checking)
    "TRY003",
    "S101",
]
# 3. Avoid trying to fix flake8-bugbear `B`) violations.
# unfixable = ["B"]

# Allow fix for all enabled rules (when `--fix`) is provided.
fixable = ["ALL"]
unfixable = []

# Allow unused variables when underscore-prefixed.
dummy-variable-rgx = "^(_+|(_+[a-zA-Z0-9_]*[a-zA-Z0-9]+?))$"

# 4. Ignore `E402` (import violations) in all `__init__.py` files, and in selected subdirectories.
#[lint.per-file-ignores]
#"__init__.py" = ["E402"]
#"**/{tests,docs,tools}/*" = ["E402"]

[lint.mccabe]
# Flag errors `C901`) whenever the complexity level exceeds 15.
max-complexity = 15